/**
 * @kepenk/templates — Elektrikçi Section Components
 * 1. ElectricianServiceGrid  2. ElectricianEmergencyBanner  3. ElectricianPricingList  4. ElectricianStatsRow  5. ElectricianCallForm
 */
'use client'
import { useState } from 'react'
import type { ComponentType } from 'react'
import type { SectionProps } from '../../types/section-types'
import { registerSection } from '../../registry/section-registry'

interface ElecSvc { id: string; emoji: string; name: string; description: string; price?: string; badge?: string }
interface ElecServiceGridC { badge?: string; title: string; subtitle?: string; services: ElecSvc[] }
function ElectricianServiceGrid({ content }: SectionProps<ElecServiceGridC>) {
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-8" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.services||[]).map(s=><div key={s.id} className="rounded-2xl border p-5 hover:shadow-md transition-shadow relative" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}>{s.badge&&<span className="absolute top-3 right-3 text-xs font-bold px-2 py-0.5 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{s.badge}</span>}<span className="text-4xl mb-3 block">{s.emoji}</span><h3 className="font-heading font-bold text-base mb-1" style={{ color: 'var(--color-text)' }}>{s.name}</h3><p className="text-sm leading-relaxed mb-2" style={{ color: 'var(--color-text-secondary)' }}>{s.description}</p>{s.price&&<span className="text-sm font-bold" style={{ color: 'var(--color-accent)' }}>{s.price}</span>}</div>)}</div></div></section>)
}
registerSection('services','electrician_services',ElectricianServiceGrid as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface ElecEmergC { title: string; subtitle: string; phone: string; features?: string[] }
function ElectricianEmergencyBanner({ content }: SectionProps<ElecEmergC>) {
  return (<section className="py-8 px-4" style={{ background: 'var(--color-accent)' }}><div className="mx-auto text-center" style={{ maxWidth: '700px' }}><h3 className="text-2xl font-heading font-bold mb-2" style={{ color: 'var(--color-text-on-accent)' }}>{content.title}</h3><p className="text-sm mb-3 opacity-90" style={{ color: 'var(--color-text-on-accent)' }}>{content.subtitle}</p><a href={`tel:${content.phone}`} className="inline-block px-6 py-3 rounded-full font-bold text-sm" style={{ background: 'var(--color-bg)', color: 'var(--color-accent)' }}>📞 {content.phone}</a>{content.features&&<div className="flex flex-wrap justify-center gap-3 mt-4">{content.features.map((f,i)=><span key={i} className="text-xs opacity-80" style={{ color: 'var(--color-text-on-accent)' }}>✓ {f}</span>)}</div>}</div></section>)
}
registerSection('social_proof','electrician_emergency',ElectricianEmergencyBanner as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface ElecPrice { service: string; price: string }
interface ElecPricingListC { badge?: string; title: string; items: ElecPrice[] }
function ElectricianPricingList({ content }: SectionProps<ElecPricingListC>) {
  return (<section className="py-12 px-4" style={{ background: 'var(--color-accent-light)' }}><div className="mx-auto" style={{ maxWidth: '600px' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-2xl font-heading font-bold mb-6" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="rounded-2xl border overflow-hidden" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}>{(content.items||[]).map((p,i)=><div key={i} className="flex items-center justify-between px-5 py-3" style={{ borderBottom: i<(content.items||[]).length-1?'1px solid var(--color-border)':'none' }}><span className="text-sm font-semibold" style={{ color: 'var(--color-text)' }}>{p.service}</span><span className="text-lg font-bold" style={{ color: 'var(--color-accent)' }}>{p.price}</span></div>)}</div></div></section>)
}
registerSection('booking','electrician_pricing',ElectricianPricingList as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface ElecStat { value: string; label: string; icon: string }
interface ElecStatsRowC { stats: ElecStat[] }
function ElectricianStatsRow({ content }: SectionProps<ElecStatsRowC>) {
  return (<section className="py-10 px-4" style={{ background: 'var(--color-accent)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}><div className="flex flex-wrap justify-center gap-8 md:gap-14">{(content.stats||[]).map((s,i)=><div key={i} className="text-center"><p className="text-3xl mb-1">{s.icon}</p><p className="text-2xl md:text-3xl font-heading font-bold" style={{ color: 'var(--color-text-on-accent)' }}>{s.value}</p><p className="text-xs opacity-80" style={{ color: 'var(--color-text-on-accent)' }}>{s.label}</p></div>)}</div></div></section>)
}
registerSection('social_proof','electrician_stats_row',ElectricianStatsRow as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface ElecCallC { badge?: string; title: string; subtitle?: string; serviceTypes?: string[]; whatsapp?: string; phone?: string; note?: string }
function ElectricianCallForm({ content }: SectionProps<ElecCallC>) {
  const [form, setForm] = useState({ name: '', phone: '', service: '', address: '', urgency: '' })
  const [sent, setSent] = useState(false)
  const update = (k: string, v: string) => setForm(p => ({ ...p, [k]: v }))
  const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if(!form.name||!form.phone)return; const wa=content.whatsapp||content.phone?.replace(/\D/g,'')||''; const msg=`Elektrik Talebi%0AAdi: ${encodeURIComponent(form.name)}%0ATel: ${form.phone}${form.service?`%0AHizmet: ${encodeURIComponent(form.service)}`:''}${form.address?`%0AAdres: ${encodeURIComponent(form.address)}`:''}${form.urgency?`%0AAciliyet: ${encodeURIComponent(form.urgency)}`:''}`; window.open(`https://wa.me/${wa}?text=${msg}`,'_blank'); setSent(true) }
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: '580px' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-6" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}{sent?<div className="text-center py-12 rounded-2xl" style={{ background: 'var(--color-bg)' }}><div className="text-5xl mb-4">⚡✅</div><h3 className="text-xl font-bold mb-2" style={{ color: 'var(--color-text)' }}>Talebiniz Alindi!</h3></div>:<form onSubmit={handleSubmit} className="rounded-2xl p-6 space-y-4" style={{ background: 'var(--color-bg)' }}><div className="grid grid-cols-1 sm:grid-cols-2 gap-4"><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Ad Soyad *</label><input type="text" required value={form.name} onChange={e=>update('name',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adiniz" /></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Telefon *</label><input type="tel" required value={form.phone} onChange={e=>update('phone',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="0555 000 00 00" /></div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{content.serviceTypes&&<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Hizmet</label><select value={form.service} onChange={e=>update('service',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }}><option value="">Seciniz…</option>{content.serviceTypes.map(t=><option key={t} value={t}>{t}</option>)}</select></div>}<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Aciliyet</label><select value={form.urgency} onChange={e=>update('urgency',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }}><option value="">Seciniz…</option><option value="Normal">Normal</option><option value="Acil">Acil</option><option value="Cok Acil">Cok Acil</option></select></div></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Adres</label><input type="text" value={form.address} onChange={e=>update('address',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adresiniz" /></div><button type="submit" className="w-full py-3 rounded-full font-bold text-sm transition-opacity hover:opacity-90" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>⚡ Elektrikci Cagir (WhatsApp)</button>{content.note&&<p className="text-xs text-center" style={{ color: 'var(--color-text-muted)' }}>{content.note}</p>}</form>}</div></section>)
}
registerSection('booking','electrician_call',ElectricianCallForm as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

export { ElectricianServiceGrid, ElectricianEmergencyBanner, ElectricianPricingList, ElectricianStatsRow, ElectricianCallForm }
