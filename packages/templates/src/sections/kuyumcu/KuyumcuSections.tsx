/**
 * @kepenk/templates — Kuyumcu Section Components
 * 1. JewelryProductGrid 2. JewelryCollections 3. JewelryMaterialBar 4. JewelryStatsRow 5. JewelryInquiryForm
 */
'use client'
import { useState } from 'react'
import type { ComponentType } from 'react'
import type { SectionProps } from '../../types/section-types'
import { registerSection } from '../../registry/section-registry'

interface JewelrySvc { id: string; emoji: string; name: string; description: string; price?: string; badge?: string }
interface JewelryProductGridC { badge?: string; title: string; subtitle?: string; products: JewelrySvc[] }
function JewelryProductGrid({ content }: SectionProps<JewelryProductGridC>) {
 return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-8" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.products||[]).map(s=><div key={s.id} className="rounded-2xl border p-5 hover:shadow-md transition-shadow relative" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}>{s.badge&&<span className="absolute top-3 right-3 text-xs font-bold px-2 py-0.5 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{s.badge}</span>}<span className="text-4xl mb-3 block">{s.emoji}</span><h3 className="font-heading font-bold text-base mb-1" style={{ color: 'var(--color-text)' }}>{s.name}</h3><p className="text-sm leading-relaxed mb-2" style={{ color: 'var(--color-text-secondary)' }}>{s.description}</p>{s.price&&<span className="text-sm font-bold" style={{ color: 'var(--color-accent)' }}>{s.price}</span>}</div>)}</div></div></section>)
}
registerSection('services','jewelry_products',JewelryProductGrid as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface Collection { id: string; emoji: string; name: string; count: string }
interface JewelryCollectionsC { badge?: string; title: string; collections: Collection[] }
function JewelryCollections({ content }: SectionProps<JewelryCollectionsC>) {
 return (<section className="py-12 px-4" style={{ background: 'var(--color-accent-light)' }}><div className="mx-auto text-center" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-2xl font-heading font-bold mb-6" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="flex flex-wrap justify-center gap-4">{(content.collections||[]).map(c=><div key={c.id} className="rounded-xl border px-5 py-4 text-center min-w-[120px]" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}><span className="text-3xl block mb-2">{c.emoji}</span><p className="text-xs font-bold" style={{ color: 'var(--color-text)' }}>{c.name}</p><p className="text-xs" style={{ color: 'var(--color-text-muted)' }}>{c.count}</p></div>)}</div></div></section>)
}
registerSection('gallery','jewelry_collections',JewelryCollections as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface Material { name: string; emoji: string; description: string }
interface JewelryMaterialBarC { badge?: string; title: string; materials: Material[] }
function JewelryMaterialBar({ content }: SectionProps<JewelryMaterialBarC>) {
 return (<section className="py-10 px-4" style={{ background: 'var(--color-bg)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-2xl font-heading font-bold mb-6" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="flex flex-wrap justify-center gap-4">{(content.materials||[]).map((m,i)=><div key={i} className="rounded-xl border px-4 py-3 text-center min-w-[100px]" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)' }}><span className="text-2xl block mb-1">{m.emoji}</span><p className="text-xs font-bold" style={{ color: 'var(--color-text)' }}>{m.name}</p><p className="text-xs" style={{ color: 'var(--color-text-muted)' }}>{m.description}</p></div>)}</div></div></section>)
}
registerSection('gallery','jewelry_materials',JewelryMaterialBar as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface JewelryStat { value: string; label: string; icon: string }
interface JewelryStatsRowC { stats: JewelryStat[] }
function JewelryStatsRow({ content }: SectionProps<JewelryStatsRowC>) {
 return (<section className="py-10 px-4" style={{ background: 'var(--color-accent)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}><div className="flex flex-wrap justify-center gap-8 md:gap-14">{(content.stats||[]).map((s,i)=><div key={i} className="text-center"><p className="text-3xl mb-1">{s.icon}</p><p className="text-2xl md:text-3xl font-heading font-bold" style={{ color: 'var(--color-text-on-accent)' }}>{s.value}</p><p className="text-xs opacity-80" style={{ color: 'var(--color-text-on-accent)' }}>{s.label}</p></div>)}</div></div></section>)
}
registerSection('social_proof','jewelry_stats_row',JewelryStatsRow as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface JewelryInquiryC { badge?: string; title: string; subtitle?: string; productTypes?: string[]; whatsapp?: string; phone?: string; note?: string }
function JewelryInquiryForm({ content }: SectionProps<JewelryInquiryC>) {
 const [form, setForm] = useState({ name: '', phone: '', product: '', budget: '', message: '' })
 const [sent, setSent] = useState(false)
 const update = (k: string, v: string) => setForm(p => ({ ...p, [k]: v }))
 const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if(!form.name||!form.phone)return; const wa=content.whatsapp||content.phone?.replace(/\D/g,'')||''; const msg=`Kuyumcu%0AAdi: ${encodeURIComponent(form.name)}%0ATel: ${form.phone}${form.product?`%0AUrun: ${encodeURIComponent(form.product)}`:''}${form.budget?`%0AButce: ${encodeURIComponent(form.budget)}`:''}${form.message?`%0ANot: ${encodeURIComponent(form.message)}`:''}`; window.open(`https://wa.me/${wa}?text=${msg}`,'_blank'); setSent(true) }
 return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: '580px' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-6" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}{sent?<div className="text-center py-12 rounded-2xl" style={{ background: 'var(--color-bg)' }}><div className="text-5xl mb-4">💎✅</div><h3 className="text-xl font-bold mb-2" style={{ color: 'var(--color-text)' }}>Talebiniz Alindi!</h3></div>:<form onSubmit={handleSubmit} className="rounded-2xl p-6 space-y-4" style={{ background: 'var(--color-bg)' }}><div className="grid grid-cols-1 sm:grid-cols-2 gap-4"><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Ad Soyad *</label><input type="text" required value={form.name} onChange={e=>update('name',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adiniz" /></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Telefon *</label><input type="tel" required value={form.phone} onChange={e=>update('phone',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="0555 000 00 00" /></div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{content.productTypes&&<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Urun</label><select value={form.product} onChange={e=>update('product',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }}><option value="">Seciniz…</option>{content.productTypes.map(t=><option key={t} value={t}>{t}</option>)}</select></div>}<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Butce</label><input type="text" value={form.budget} onChange={e=>update('budget',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Butce araligini belirtiniz" /></div></div><button type="submit" className="w-full py-3 rounded-full font-bold text-sm transition-opacity hover:opacity-90" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>💎 Bilgi Al (WhatsApp)</button>{content.note&&<p className="text-xs text-center" style={{ color: 'var(--color-text-muted)' }}>{content.note}</p>}</form>}</div></section>)
}
registerSection('booking','jewelry_inquiry',JewelryInquiryForm as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

export { JewelryProductGrid, JewelryCollections, JewelryMaterialBar, JewelryStatsRow, JewelryInquiryForm }
