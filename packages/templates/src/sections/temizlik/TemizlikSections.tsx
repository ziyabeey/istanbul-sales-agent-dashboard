/**
 * @kepenk/templates — Temizlik Şirketi Section Components
 * 1. CleaningServiceGrid  2. CleaningPackages  3. CleaningProcessSteps  4. CleaningStatsRow  5. CleaningBookingForm
 */
'use client'
import { useState } from 'react'
import type { ComponentType } from 'react'
import type { SectionProps } from '../../types/section-types'
import { registerSection } from '../../registry/section-registry'

interface CleanSvc { id: string; emoji: string; name: string; description: string; priceUnit?: string; badge?: string }
interface CleaningServiceGridC { badge?: string; title: string; subtitle?: string; services: CleanSvc[] }
function CleaningServiceGrid({ content }: SectionProps<CleaningServiceGridC>) {
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-8" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.services||[]).map(s=><div key={s.id} className="rounded-2xl border p-5 hover:shadow-md transition-shadow relative" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}>{s.badge&&<span className="absolute top-3 right-3 text-xs font-bold px-2 py-0.5 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{s.badge}</span>}<span className="text-4xl mb-3 block">{s.emoji}</span><h3 className="font-heading font-bold text-base mb-1" style={{ color: 'var(--color-text)' }}>{s.name}</h3><p className="text-sm leading-relaxed mb-2" style={{ color: 'var(--color-text-secondary)' }}>{s.description}</p>{s.priceUnit&&<span className="text-sm font-bold" style={{ color: 'var(--color-accent)' }}>{s.priceUnit}</span>}</div>)}</div></div></section>)
}
registerSection('services','cleaning_services',CleaningServiceGrid as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface CleanPkg { id: string; emoji: string; name: string; price: string; features: string[]; badge?: string; highlighted?: boolean }
interface CleaningPackagesC { badge?: string; title: string; packages: CleanPkg[] }
function CleaningPackages({ content }: SectionProps<CleaningPackagesC>) {
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-bg)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-8" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.packages||[]).map(p=><div key={p.id} className="rounded-2xl border p-6 text-center relative" style={{ background: p.highlighted?'var(--color-accent-light)':'var(--color-surface)', borderColor: p.highlighted?'var(--color-accent)':'var(--color-border)' }}>{p.badge&&<span className="absolute -top-3 left-1/2 -translate-x-1/2 text-xs font-bold px-3 py-1 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{p.badge}</span>}<span className="text-4xl block mb-3">{p.emoji}</span><h3 className="font-heading font-bold text-lg" style={{ color: 'var(--color-text)' }}>{p.name}</h3><div className="my-3"><span className="text-3xl font-bold" style={{ color: 'var(--color-accent)' }}>{p.price}</span></div><ul className="space-y-1.5 mb-4 text-left">{p.features.map((f,i)=><li key={i} className="text-sm flex items-start gap-2" style={{ color: 'var(--color-text-secondary)' }}><span className="text-green-500 mt-0.5">✓</span> {f}</li>)}</ul></div>)}</div></div></section>)
}
registerSection('booking','cleaning_packages',CleaningPackages as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface CStep { step: number; emoji: string; title: string; description: string }
interface CleaningProcessStepsC { badge?: string; title: string; steps: CStep[] }
function CleaningProcessSteps({ content }: SectionProps<CleaningProcessStepsC>) {
  return (<section className="py-12 px-4" style={{ background: 'var(--color-accent-light)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-2xl font-heading font-bold mb-6" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="flex flex-wrap justify-center gap-4">{(content.steps||[]).map(s=><div key={s.step} className="rounded-xl border px-5 py-4 text-center min-w-[130px] relative" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}><span className="absolute -top-2 -left-2 w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{s.step}</span><span className="text-3xl block mb-2">{s.emoji}</span><p className="text-xs font-bold" style={{ color: 'var(--color-text)' }}>{s.title}</p><p className="text-xs mt-1" style={{ color: 'var(--color-text-muted)' }}>{s.description}</p></div>)}</div></div></section>)
}
registerSection('gallery','cleaning_process',CleaningProcessSteps as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface CleanStat { value: string; label: string; icon: string }
interface CleaningStatsRowC { stats: CleanStat[] }
function CleaningStatsRow({ content }: SectionProps<CleaningStatsRowC>) {
  return (<section className="py-10 px-4" style={{ background: 'var(--color-accent)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}><div className="flex flex-wrap justify-center gap-8 md:gap-14">{(content.stats||[]).map((s,i)=><div key={i} className="text-center"><p className="text-3xl mb-1">{s.icon}</p><p className="text-2xl md:text-3xl font-heading font-bold" style={{ color: 'var(--color-text-on-accent)' }}>{s.value}</p><p className="text-xs opacity-80" style={{ color: 'var(--color-text-on-accent)' }}>{s.label}</p></div>)}</div></div></section>)
}
registerSection('social_proof','cleaning_stats_row',CleaningStatsRow as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface CleaningBookingC { badge?: string; title: string; subtitle?: string; serviceTypes?: string[]; whatsapp?: string; phone?: string; note?: string }
function CleaningBookingForm({ content }: SectionProps<CleaningBookingC>) {
  const [form, setForm] = useState({ name: '', phone: '', service: '', address: '', date: '' })
  const [sent, setSent] = useState(false)
  const update = (k: string, v: string) => setForm(p => ({ ...p, [k]: v }))
  const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if(!form.name||!form.phone)return; const wa=content.whatsapp||content.phone?.replace(/\D/g,'')||''; const msg=`Temizlik Talebi%0AAdi: ${encodeURIComponent(form.name)}%0ATel: ${form.phone}${form.service?`%0AHizmet: ${encodeURIComponent(form.service)}`:''}${form.address?`%0AAdres: ${encodeURIComponent(form.address)}`:''}${form.date?`%0ATarih: ${form.date}`:''}`; window.open(`https://wa.me/${wa}?text=${msg}`,'_blank'); setSent(true) }
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: '580px' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-6" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}{sent?<div className="text-center py-12 rounded-2xl" style={{ background: 'var(--color-bg)' }}><div className="text-5xl mb-4">🧹✅</div><h3 className="text-xl font-bold mb-2" style={{ color: 'var(--color-text)' }}>Talebiniz Alindi!</h3></div>:<form onSubmit={handleSubmit} className="rounded-2xl p-6 space-y-4" style={{ background: 'var(--color-bg)' }}><div className="grid grid-cols-1 sm:grid-cols-2 gap-4"><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Ad Soyad *</label><input type="text" required value={form.name} onChange={e=>update('name',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adiniz" /></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Telefon *</label><input type="tel" required value={form.phone} onChange={e=>update('phone',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="0555 000 00 00" /></div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{content.serviceTypes&&<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Hizmet</label><select value={form.service} onChange={e=>update('service',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }}><option value="">Seciniz…</option>{content.serviceTypes.map(t=><option key={t} value={t}>{t}</option>)}</select></div>}<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Tarih</label><input type="date" value={form.date} onChange={e=>update('date',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} /></div></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Adres</label><input type="text" value={form.address} onChange={e=>update('address',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adresiniz" /></div><button type="submit" className="w-full py-3 rounded-full font-bold text-sm transition-opacity hover:opacity-90" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>🧹 Randevu Al (WhatsApp)</button>{content.note&&<p className="text-xs text-center" style={{ color: 'var(--color-text-muted)' }}>{content.note}</p>}</form>}</div></section>)
}
registerSection('booking','cleaning_booking',CleaningBookingForm as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

export { CleaningServiceGrid, CleaningPackages, CleaningProcessSteps, CleaningStatsRow, CleaningBookingForm }
