/**
 * @kepenk/templates — Organizasyon Section Components
 * 1. EventServiceGrid  2. EventPackages  3. EventGalleryGrid  4. EventStatsRow  5. EventBookingForm
 */
'use client'
import { useState } from 'react'
import type { ComponentType } from 'react'
import type { SectionProps } from '../../types/section-types'
import { registerSection } from '../../registry/section-registry'

interface EventSvc { id: string; emoji: string; name: string; description: string; badge?: string }
interface EventServiceGridC { badge?: string; title: string; subtitle?: string; services: EventSvc[] }
function EventServiceGrid({ content }: SectionProps<EventServiceGridC>) {
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-8" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.services||[]).map(s=><div key={s.id} className="rounded-2xl border p-5 hover:shadow-md transition-shadow relative" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}>{s.badge&&<span className="absolute top-3 right-3 text-xs font-bold px-2 py-0.5 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{s.badge}</span>}<span className="text-4xl mb-3 block">{s.emoji}</span><h3 className="font-heading font-bold text-base mb-1" style={{ color: 'var(--color-text)' }}>{s.name}</h3><p className="text-sm leading-relaxed" style={{ color: 'var(--color-text-secondary)' }}>{s.description}</p></div>)}</div></div></section>)
}
registerSection('services','event_services',EventServiceGrid as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface EventPkg { id: string; emoji: string; name: string; price: string; features: string[]; badge?: string; highlighted?: boolean }
interface EventPackagesC { badge?: string; title: string; packages: EventPkg[] }
function EventPackages({ content }: SectionProps<EventPackagesC>) {
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-bg)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-8" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">{(content.packages||[]).map(p=><div key={p.id} className="rounded-2xl border p-6 text-center relative" style={{ background: p.highlighted?'var(--color-accent-light)':'var(--color-surface)', borderColor: p.highlighted?'var(--color-accent)':'var(--color-border)' }}>{p.badge&&<span className="absolute -top-3 left-1/2 -translate-x-1/2 text-xs font-bold px-3 py-1 rounded-full" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>{p.badge}</span>}<span className="text-4xl block mb-3">{p.emoji}</span><h3 className="font-heading font-bold text-lg" style={{ color: 'var(--color-text)' }}>{p.name}</h3><div className="my-3"><span className="text-3xl font-bold" style={{ color: 'var(--color-accent)' }}>{p.price}</span></div><ul className="space-y-1.5 mb-4 text-left">{p.features.map((f,i)=><li key={i} className="text-sm flex items-start gap-2" style={{ color: 'var(--color-text-secondary)' }}><span className="text-green-500 mt-0.5">✓</span> {f}</li>)}</ul></div>)}</div></div></section>)
}
registerSection('booking','event_packages',EventPackages as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface GalleryItem { id: string; emoji: string; title: string; category?: string }
interface EventGalleryGridC { badge?: string; title: string; items: GalleryItem[] }
function EventGalleryGrid({ content }: SectionProps<EventGalleryGridC>) {
  return (<section className="py-12 px-4" style={{ background: 'var(--color-accent-light)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-2xl font-heading font-bold mb-6" style={{ color: 'var(--color-text)' }}>{content.title}</h2><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">{(content.items||[]).map(item=><div key={item.id} className="rounded-xl border p-4 text-center" style={{ background: 'var(--color-bg)', borderColor: 'var(--color-border)' }}><span className="text-4xl block mb-2">{item.emoji}</span><p className="text-xs font-bold" style={{ color: 'var(--color-text)' }}>{item.title}</p>{item.category&&<p className="text-xs" style={{ color: 'var(--color-text-muted)' }}>{item.category}</p>}</div>)}</div></div></section>)
}
registerSection('gallery','event_gallery',EventGalleryGrid as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface EventStat { value: string; label: string; icon: string }
interface EventStatsRowC { stats: EventStat[] }
function EventStatsRow({ content }: SectionProps<EventStatsRowC>) {
  return (<section className="py-10 px-4" style={{ background: 'var(--color-accent)' }}><div className="mx-auto" style={{ maxWidth: 'var(--container-lg, 1024px)' }}><div className="flex flex-wrap justify-center gap-8 md:gap-14">{(content.stats||[]).map((s,i)=><div key={i} className="text-center"><p className="text-3xl mb-1">{s.icon}</p><p className="text-2xl md:text-3xl font-heading font-bold" style={{ color: 'var(--color-text-on-accent)' }}>{s.value}</p><p className="text-xs opacity-80" style={{ color: 'var(--color-text-on-accent)' }}>{s.label}</p></div>)}</div></div></section>)
}
registerSection('social_proof','event_stats_row',EventStatsRow as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

interface EventBookingC { badge?: string; title: string; subtitle?: string; eventTypes?: string[]; whatsapp?: string; phone?: string; note?: string }
function EventBookingForm({ content }: SectionProps<EventBookingC>) {
  const [form, setForm] = useState({ name: '', phone: '', type: '', date: '', guests: '', message: '' })
  const [sent, setSent] = useState(false)
  const update = (k: string, v: string) => setForm(p => ({ ...p, [k]: v }))
  const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if(!form.name||!form.phone)return; const wa=content.whatsapp||content.phone?.replace(/\D/g,'')||''; const msg=`Organizasyon%0AAdi: ${encodeURIComponent(form.name)}%0ATel: ${form.phone}${form.type?`%0ATur: ${encodeURIComponent(form.type)}`:''}${form.date?`%0ATarih: ${form.date}`:''}${form.guests?`%0AKisi: ${form.guests}`:''}${form.message?`%0ANot: ${encodeURIComponent(form.message)}`:''}`; window.open(`https://wa.me/${wa}?text=${msg}`,'_blank'); setSent(true) }
  return (<section className="py-16 md:py-24 px-4" style={{ background: 'var(--color-surface)' }}><div className="mx-auto" style={{ maxWidth: '580px' }}>{content.badge&&<span className="inline-block text-xs font-semibold tracking-widest uppercase mb-3" style={{ color: 'var(--color-accent)' }}>{content.badge}</span>}<h2 className="text-3xl md:text-4xl font-heading font-bold mb-2" style={{ color: 'var(--color-text)' }}>{content.title}</h2>{content.subtitle&&<p className="text-base mb-6" style={{ color: 'var(--color-text-secondary)' }}>{content.subtitle}</p>}{sent?<div className="text-center py-12 rounded-2xl" style={{ background: 'var(--color-bg)' }}><div className="text-5xl mb-4">🎉✅</div><h3 className="text-xl font-bold mb-2" style={{ color: 'var(--color-text)' }}>Talebiniz Alindi!</h3></div>:<form onSubmit={handleSubmit} className="rounded-2xl p-6 space-y-4" style={{ background: 'var(--color-bg)' }}><div className="grid grid-cols-1 sm:grid-cols-2 gap-4"><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Ad Soyad *</label><input type="text" required value={form.name} onChange={e=>update('name',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="Adiniz" /></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Telefon *</label><input type="tel" required value={form.phone} onChange={e=>update('phone',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="0555 000 00 00" /></div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{content.eventTypes&&<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Etkinlik</label><select value={form.type} onChange={e=>update('type',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }}><option value="">Seciniz…</option>{content.eventTypes.map(t=><option key={t} value={t}>{t}</option>)}</select></div>}<div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Tarih</label><input type="date" value={form.date} onChange={e=>update('date',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} /></div></div><div><label className="block text-sm font-semibold mb-1" style={{ color: 'var(--color-text)' }}>Kisi Sayisi</label><input type="number" value={form.guests} onChange={e=>update('guests',e.target.value)} className="w-full px-4 py-2.5 rounded-xl border text-sm outline-none" style={{ background: 'var(--color-surface)', borderColor: 'var(--color-border)', color: 'var(--color-text)' }} placeholder="50" /></div><button type="submit" className="w-full py-3 rounded-full font-bold text-sm transition-opacity hover:opacity-90" style={{ background: 'var(--color-accent)', color: 'var(--color-text-on-accent)' }}>🎉 Teklif Al (WhatsApp)</button>{content.note&&<p className="text-xs text-center" style={{ color: 'var(--color-text-muted)' }}>{content.note}</p>}</form>}</div></section>)
}
registerSection('booking','event_booking',EventBookingForm as unknown as ComponentType<SectionProps<Record<string,unknown>>>)

export { EventServiceGrid, EventPackages, EventGalleryGrid, EventStatsRow, EventBookingForm }
